# Product

## Register

product

## Users

Windows上で定型画像の同じ位置から文字を読み取り、Excelへ転記する作業者。主な利用者は、画像一覧を行データへ変換する事務・分析・記録担当者で、テンプレートを一度作ってから同形式の画像フォルダをまとめて処理する。

利用時の文脈は、サンプル画像を見ながら読み取り範囲を定義し、OCR結果を確認し、列順と出力条件を整えてExcelファイルを生成する作業中である。速度よりも、読み取り位置・列順・出力先を間違えないことが重要。

## Product Purpose

Image OCR to Excelは、固定フォーマット画像の指定領域をOCRし、画像1枚をExcelの1行として出力するローカル生産性ツールである。

成功状態は、ユーザーが以下を迷わず完了できること。

- 1枚のサンプル画像から再利用できる読み取りテンプレートを作る。
- OCR結果を画像ごと・項目ごとに確認し、必要なら手修正する。
- Excelの列順、シート名、開始セル、追記/上書きなどを明示的に確認して出力する。
- エラーがあっても処理が止まらず、あとから原因を追える。

## Brand Personality

正確・静か・堅実。

見た目は白と淡い青緑を基調にし、ティールを操作・フォーカス、オレンジを出力・選択領域のアクセントとして使う。色は装飾ではなく、作業状態と重要な操作を見分けるための機能色として扱う。

## Anti-references

装飾的なSaaS風の見た目にはしない。特に、派手なヒーロー、カードだらけのランディングページ風構成、過剰なグラデーション、説明的なマーケティングコピー、実作業と関係ない装飾を避ける。

アプリはExcelそのものの再現でも、開発者向けターミナルでもない。主役は画像ワークスペース、読み取り領域、OCR結果、Excel出力条件である。

## Design Principles

1. ワークフローを狭く保つ: テンプレート作成、OCR確認、Excel出力に直接関係しない機能は二次的に扱う。
2. 信頼できる状態を常に見せる: 選択中の画像、項目数、列順、出力対象、エラー、進行状況を隠さない。
3. 画像編集と表データの関係を切らない: 読み取り領域、項目名、Excel列順が同じ文脈で理解できるようにする。
4. 破壊的・不可逆な操作は保守的にする: 上書き、削除、出力先変更は明示的なラベルと確認できる状態を持つ。
5. 処理境界を守る: UIはOCR、Excel書き込み、テンプレート保存、座標スケーリングを再実装せず、既存の処理モジュールを呼び出す。

## Accessibility & Inclusion

WCAG AA相当を基準にする。通常テキストは背景に対して4.5:1以上、重要なUI状態は色だけで区別しない。キーボード操作、フォーカス表示、十分なクリック/ドラッグ対象サイズ、読み取りやすい日本語UIを重視する。

モーションは状態変化の理解に必要な範囲に限定し、減速モーション設定では即時または最小限の遷移にする。ダークモードでは低コントラストの灰色文字を避け、長時間作業でも目が疲れにくい明度差を保つ。
