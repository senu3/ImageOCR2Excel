# Archive

このフォルダーには、基盤移行前の実装と、採用を見送ったTauri試作を保管する。
現行アプリの実行・修正対象は、ルート直下の起動スクリプトと
`ImageOCR2Excel/`パッケージである。

- `legacy_mvp/`: 移行前のPython単体MVP
- `tauri_prototype/`: React/Tauri試作と、その専用設計資料

`node_modules`、`dist`、Rustの`target`、実行ログなどの生成物は履歴へ含めない。
