{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the Image OCR to Excel window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
